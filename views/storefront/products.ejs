<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head') %>
</head>

<body class="bg-gray-900 text-gray-100 min-h-screen">
    <%- include('../partials/storefront_nav') %>

        <main class="bg-gray-900 min-h-screen">
            <!-- Page Hero -->
            <div class="relative bg-gradient-to-b from-gray-800/50 to-gray-900 border-b border-gray-800">
                <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
                    <div class="flex items-end justify-between">
                        <div>
                            <nav class="flex items-center text-sm text-gray-500 mb-3">
                                <a href="/" class="hover:text-yellow-500 transition-colors">Home</a>
                                <svg class="w-4 h-4 mx-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M9 5l7 7-7 7" />
                                </svg>
                                <span class="text-gray-300">Products</span>
                            </nav>
                            <h1 class="text-4xl font-bold text-white font-display">
                                <% if (query.q) { %>
                                    Search: "<span class="text-yellow-500">
                                        <%= query.q %>
                                    </span>"
                                    <% } else if (query.category) { %>
                                        <%= categories.find(c=> c.slug === query.category)?.name || 'Products' %>
                                            <% } else { %>
                                                All Products
                                                <% } %>
                            </h1>
                        </div>
                        <span
                            class="text-gray-400 text-sm font-medium bg-gray-800 px-4 py-2 rounded-full border border-gray-700">
                            <%= totalItems %> results
                        </span>
                    </div>
                </div>
            </div>

            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
                <div class="flex gap-8">
                    <!-- Sidebar Filters -->
                    <aside class="w-64 flex-shrink-0 hidden lg:block">
                        <div
                            class="bg-gray-800/50 border border-gray-700 rounded-xl p-6 sticky top-28 backdrop-blur-sm">
                            <h3 class="text-sm font-bold text-yellow-500 uppercase tracking-wider mb-5">Categories</h3>
                            <ul class="space-y-1">
                                <li>
                                    <a href="/products"
                                        class="flex items-center px-3 py-2.5 rounded-lg text-sm font-medium transition-all duration-200 <%= !query.category ? 'bg-yellow-500/10 text-yellow-400 border border-yellow-500/30' : 'text-gray-400 hover:text-gray-200 hover:bg-gray-700/50' %>">
                                        <svg class="w-4 h-4 mr-3 flex-shrink-0" fill="none" stroke="currentColor"
                                            viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zm10 0a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zm10 0a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z" />
                                        </svg>
                                        All Products
                                    </a>
                                </li>
                                <% categories.forEach(cat=> { %>
                                    <li>
                                        <a href="/products?category=<%= cat.slug %>"
                                            class="flex items-center px-3 py-2.5 rounded-lg text-sm font-medium transition-all duration-200 <%= query.category === cat.slug ? 'bg-yellow-500/10 text-yellow-400 border border-yellow-500/30' : 'text-gray-400 hover:text-gray-200 hover:bg-gray-700/50' %>">
                                            <span
                                                class="w-2 h-2 rounded-full bg-yellow-500/50 mr-3 flex-shrink-0"></span>
                                            <%= cat.name %>
                                        </a>
                                    </li>
                                    <% }); %>
                            </ul>
                        </div>
                    </aside>

                    <!-- Products Grid -->
                    <div class="flex-1">
                        <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-6">
                            <% if (products.length> 0) { %>
                                <% products.forEach(product=> { %>
                                    <div class="group relative bg-gray-800 rounded-xl border border-gray-700 overflow-hidden hover:border-yellow-500/50 transition-all duration-300 hover:shadow-2xl hover:shadow-yellow-500/10 hover:-translate-y-1 product-card"
                                        data-product-id="<%= product.id %>">
                                        <!-- Card Image -->
                                        <a href="/products/<%= product.slug %>"
                                            class="block relative aspect-[3/4] bg-gray-900/50 p-4 cursor-pointer">
                                            <img src="<%= product.imageUrl %>" alt="<%= product.name %>"
                                                class="w-full h-full object-contain drop-shadow-2xl transition-transform duration-500 group-hover:scale-110"
                                                loading="lazy">
                                            <% if (product.isFoil) { %>
                                                <div class="absolute inset-0 bg-gradient-to-tr from-transparent via-white/10 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-700 pointer-events-none"
                                                    style="background-size: 200% 200%; background-position: 0% 0%;">
                                                </div>
                                                <span
                                                    class="absolute top-2 right-2 px-2 py-0.5 bg-gradient-to-r from-yellow-400 to-yellow-600 text-gray-900 text-[10px] font-black uppercase tracking-wider rounded shadow-lg">FOIL</span>
                                                <% } %>
                                        </a>

                                        <!-- Product Info -->
                                        <div class="p-4">
                                            <a href="/products/<%= product.slug %>">
                                                <h3
                                                    class="text-sm font-bold text-gray-100 mb-1 truncate group-hover:text-yellow-500 transition-colors cursor-pointer">
                                                    <%= product.name %>
                                                </h3>
                                            </a>
                                            <p class="text-xs text-gray-500 mb-3 truncate">
                                                <%= product.setName %> · <%= product.rarity || 'C' %>
                                            </p>

                                            <!-- Condition Toggles -->
                                            <div class="flex flex-wrap gap-1.5 mb-3 condition-toggles">
                                                <% if (product.stocks && product.stocks.length> 0) { %>
                                                    <% product.stocks.forEach((stock, index)=> { %>
                                                        <button
                                                            class="condition-btn px-2 py-1 text-[10px] font-bold rounded border <%= index === 0 ? 'bg-gray-700 border-gray-600 text-white' : 'bg-transparent border-gray-700 text-gray-500 hover:border-gray-500' %> transition-all"
                                                            data-condition="<%= stock.condition %>"
                                                            data-price="<%= stock.price %>"
                                                            data-stock="<%= stock.quantity %>"
                                                            data-stock-id="<%= stock.id %>">
                                                            <%= stock.condition %>
                                                        </button>
                                                        <% }); %>
                                                            <% } else { %>
                                                                <span class="text-xs text-gray-600 italic">Out of
                                                                    Stock</span>
                                                                <% } %>
                                            </div>

                                            <!-- Action -->
                                            <button
                                                class="w-full mt-2 py-2.5 bg-gray-700 hover:bg-yellow-500 text-white hover:text-gray-900 font-bold text-sm rounded-lg transition-all duration-300 flex justify-between items-center px-4 group/btn add-to-cart-btn"
                                                data-product-id="<%= product.id %>"
                                                data-stock-id="<%= product.stocks && product.stocks.length > 0 ? product.stocks[0].id : '' %>"
                                                <%=(!product.stocks || product.stocks.length===0) ? 'disabled' : '' %>>
                                                <span class="price-display text-base">$<%= (product.stocks &&
                                                        product.stocks.length> 0 ? product.stocks[0].price :
                                                        0).toFixed(2) %></span>
                                                <div class="flex items-center gap-2">
                                                    <span class="text-[10px] font-normal opacity-60 stock-display">[<%=
                                                            product.stocks && product.stocks.length> 0 ?
                                                            product.stocks[0].quantity : 0 %>]</span>
                                                    <svg class="w-4 h-4 transform group-hover/btn:translate-x-1 transition-transform"
                                                        fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                        <path stroke-linecap="round" stroke-linejoin="round"
                                                            stroke-width="2" d="M12 4v16m8-8H4" />
                                                    </svg>
                                                </div>
                                            </button>
                                        </div>
                                    </div>
                                    <% }); %>
                                        <% } else { %>
                                            <div class="col-span-full py-20 text-center">
                                                <div
                                                    class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-gray-800 text-gray-600 mb-4">
                                                    <svg class="w-8 h-8" fill="none" stroke="currentColor"
                                                        viewBox="0 0 24 24">
                                                        <path stroke-linecap="round" stroke-linejoin="round"
                                                            stroke-width="2"
                                                            d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4" />
                                                    </svg>
                                                </div>
                                                <h3 class="text-xl font-bold text-white mb-2 font-display">No Cards
                                                    Found</h3>
                                                <p class="text-gray-500">Try adjusting your search or category filters.
                                                </p>
                                            </div>
                                            <% } %>
                        </div>

                        <!-- Pagination -->
                        <% if (totalPages> 1) { %>
                            <div class="mt-12 flex justify-center items-center gap-2">
                                <% if (hasPrev) { %>
                                    <a href="/products?page=<%= prevPage %>&category=<%= query.category || '' %>&q=<%= query.q || '' %>"
                                        class="px-4 py-2 bg-gray-800 border border-gray-700 text-gray-300 rounded-lg hover:bg-gray-700 hover:text-white transition-all text-sm font-medium">←
                                        Prev</a>
                                    <% } %>

                                        <% for(let i=1; i <=totalPages; i++) { %>
                                            <% if (i===1 || i===totalPages || (i>= currentPage - 2 && i <= currentPage +
                                                    2)) { %>
                                                    <a href="/products?page=<%= i %>&category=<%= query.category || '' %>&q=<%= query.q || '' %>"
                                                        class="w-10 h-10 flex items-center justify-center rounded-lg text-sm font-medium transition-all <%= i === currentPage ? 'bg-yellow-500 text-gray-900 font-bold shadow-lg shadow-yellow-500/20' : 'bg-gray-800 border border-gray-700 text-gray-400 hover:bg-gray-700 hover:text-white' %>">
                                                        <%= i %>
                                                    </a>
                                                    <% } else if (i===currentPage - 3 || i===currentPage + 3) { %>
                                                        <span class="px-2 text-gray-600">···</span>
                                                        <% } %>
                                                            <% } %>

                                                                <% if (hasNext) { %>
                                                                    <a href="/products?page=<%= nextPage %>&category=<%= query.category || '' %>&q=<%= query.q || '' %>"
                                                                        class="px-4 py-2 bg-gray-800 border border-gray-700 text-gray-300 rounded-lg hover:bg-gray-700 hover:text-white transition-all text-sm font-medium">Next
                                                                        →</a>
                                                                    <% } %>
                            </div>
                            <% } %>
                    </div>
                </div>
            </div>
        </main>

        <%- include('../partials/storefront_footer') %>

            <script>
                // Condition toggle functionality
                document.querySelectorAll('.product-card').forEach(card => {
                    const toggles = card.querySelectorAll('.condition-btn');
                    const priceDisplay = card.querySelector('.price-display');
                    const stockDisplay = card.querySelector('.stock-display');
                    const addBtn = card.querySelector('.add-to-cart-btn');

                    toggles.forEach(toggle => {
                        toggle.addEventListener('click', function (e) {
                            e.preventDefault();
                            toggles.forEach(t => {
                                t.classList.remove('bg-gray-700', 'border-gray-600', 'text-white');
                                t.classList.add('bg-transparent', 'border-gray-700', 'text-gray-500');
                            });
                            this.classList.remove('bg-transparent', 'border-gray-700', 'text-gray-500');
                            this.classList.add('bg-gray-700', 'border-gray-600', 'text-white');

                            const price = this.dataset.price;
                            const stock = this.dataset.stock;
                            const stockId = this.dataset.stockId;

                            if (price) priceDisplay.textContent = `$${parseFloat(price).toFixed(2)}`;
                            if (stock) stockDisplay.textContent = `[${stock}]`;
                            if (stockId) {
                                addBtn.dataset.stockId = stockId;
                                addBtn.disabled = false;
                            }
                        });
                    });
                });

                // Add to cart
                document.querySelectorAll('.add-to-cart-btn').forEach(btn => {
                    btn.addEventListener('click', async function (e) {
                        e.preventDefault();
                        if (this.disabled) return;

                        const productId = this.dataset.productId;
                        const stockId = this.dataset.stockId;

                        try {
                            const originalContent = this.innerHTML;
                            this.innerHTML = `<svg class="animate-spin w-5 h-5 mx-auto" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path></svg>`;

                            const response = await fetch('/api/cart/add', {
                                method: 'POST',
                                headers: { 'Content-Type': 'application/json' },
                                body: JSON.stringify({ product_id: productId, stock_id: stockId, quantity: 1 })
                            });
                            const data = await response.json();

                            if (data.success) {
                                document.getElementById('cart-count').textContent = data.cart_count;
                                this.classList.remove('bg-gray-700', 'text-white');
                                this.classList.add('bg-green-600', 'text-white');
                                this.innerHTML = `✓ Added`;
                                setTimeout(() => {
                                    this.classList.remove('bg-green-600');
                                    this.classList.add('bg-gray-700');
                                    this.innerHTML = originalContent;
                                }, 1500);
                            } else {
                                this.innerHTML = originalContent;
                                alert(data.error);
                            }
                        } catch (err) {
                            console.error(err);
                            this.innerHTML = originalContent;
                        }
                    });
                });
            </script>
</body>

</html>