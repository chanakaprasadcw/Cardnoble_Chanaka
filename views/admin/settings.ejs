<%- include('../partials/admin_layout_start', { title: 'Settings' }) %>
    <%- include('../partials/admin_header', { title: 'Settings' }) %>

        <div class="bg-white rounded-xl shadow-sm p-6 max-w-2xl">
            <h2 class="text-xl font-semibold mb-6">Store Details</h2>
            <form class="space-y-6">
                <!-- Note: This form is currently a placeholder as per original template -->
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Store Name</label>
                    <input type="text" value="CardNoble" class="w-full px-4 py-2 border border-gray-300 rounded-lg">
                </div>
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Contact Email</label>
                    <input type="email" value="support@cardnoble.com"
                        class="w-full px-4 py-2 border border-gray-300 rounded-lg">
                </div>
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Currency</label>
                    <select class="w-full px-4 py-2 border border-gray-300 rounded-lg">
                        <option>PHP - Philippine Peso</option>
                        <option>USD - US Dollar</option>
                    </select>
                </div>
                <button type="button" onclick="alert('Settings saved (placeholder)')"
                    class="px-6 py-2 bg-indigo-600 text-white rounded-lg hover:bg-indigo-700">Save
                    Changes</button>
            </form>
        </div>

        <%- include('../partials/admin_layout_end') %>